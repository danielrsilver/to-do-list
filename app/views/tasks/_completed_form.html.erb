<%= form_with(model: task, local: true) do |form| %>
  <%= form.hidden_field :completed, value: !task.completed %>
  <%= form.submit task.completed ? "Mark Incomplete" : "Mark Complete", class: "btn btn-secondary float-right" %>
<% end %>
